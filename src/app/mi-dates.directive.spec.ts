import { MiDatesDirective } from './mi-dates.directive';

describe('MiDatesDirective', () => {
  it('should create an instance', () => {
    // const directive = new MiDatesDirective();
    // expect(directive).toBeTruthy();
  });
});
